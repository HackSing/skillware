{
  "name": "askill-search-poc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase-0 falsification skeleton for askill-search (MCP skill_search / skill_read).",
  "bin": {
    "askill-search-poc": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "smoke": "npm run build && tsx scripts/smoke.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
